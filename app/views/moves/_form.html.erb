<%= form_for @move do |f| %>
    <%= f.label :name  %>
    <%= f.text_field :name %>

    <%= f.label :category %>
    <%= f.text_field :category %>

    <%# f.label :type %>
    <%# f.text_field move.element.image %>

    <%= f.submit "Create Move" %>
<% end %>