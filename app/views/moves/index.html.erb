<h1>Index Moves</h1>

<div class="infocard">

    <% @moves.each do |move| %>
        <p>
            <%= link_to move.name, move_path(move.id) %>
            <img class="thumbnail" src="<%= move.element %>">
        </p>
    <% end %>
</div>